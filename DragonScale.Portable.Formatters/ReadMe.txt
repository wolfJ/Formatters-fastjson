

Attribute contains:
Json+-------------
	+IgnoreAttribute(user can config at settings)
	+LoopReferanceAttribute(Setting)
	+ConvertNameAttribute(can convert name, user can config at settings)
	+ConvertValueTypeAttribute(can convert value type, user can config at settings)


unrealized function:
1.struct deSerialize
2.mapping
3.test

MaxNestingDepth default is 100,

support mapping
	toJson: